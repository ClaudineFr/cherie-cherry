# --- Cœur de l'application ---
asgiref==3.12.1
Django==6.0.7
djangorestframework==3.17.1
pillow==12.3.0
sqlparse==0.5.5

# --- Ajouts pour la mise en production (Railway) ---
dj-database-url==3.1.2      # traduit DATABASE_URL en config Django (Postgres en prod, SQLite en local)
psycopg[binary]==3.3.4      # driver PostgreSQL
whitenoise==6.12.0          # sert les fichiers statiques (CSS de l'admin) en prod
gunicorn==26.0.0            # serveur d'application qui fait tourner Django en prod
